
## 一、项目概述

本项目基于阿里通义实验室开源的 **Ovis2.5-9B** 多模态大模型，面向**电力输配电巡检与运维**场景进行监督微调（SFT），使模型具备对电力巡检图像的专业分析能力，能够回答关于线路异物、绝缘子缺陷、电表计量、鸟害隐患等电力运维领域的问题。

| 项目 | 说明 |
|:---|:---|
| 基础模型 | AIDC-AI/Ovis2.5-9B |
| 模型权重路径 | `/data1/home/lwx/work/Download/modal_models/ovis` |
| 微调方式 | SFT（监督微调），部分模块训练 |
| 训练框架 | DeepSpeed ZeRO-1 + Transformers + Flash Attention 2 |
| GPU 资源 | 2 × GPU（`--nproc_per_node=2`） |
| 工作目录 | `/data1/home/lwx/work/Code/Experiment/LingShu/Ovis/` |

---

## 二、技术方案

### 2.1 基础模型架构：Ovis2.5

Ovis（Open VISion）是一种结构化的多模态大模型架构，核心设计思想是**将视觉嵌入与文本嵌入在结构上对齐（Structural Embedding Alignment）**。Ovis2.5 是该系列的最新版本，架构组成如下：

```
输入图像 → ViT (SigLIP2) → Visual Tokenizer → Visual Embedding (VTE)
                                                    ↓
用户文本 → Text Tokenizer → Text Embedding  →   [LLM: Qwen3-8B] → 输出文本
```

- **ViT（视觉编码器）**：SigLIP2 so400m-patch16-512，支持原生分辨率（NaviT），动态分块处理任意分辨率图像。
- **Visual Tokenizer**：将 ViT 输出的视觉特征通过线性层 + LayerNorm 映射到视觉词表空间，产生视觉 token。
- **VTE（Visual Token Embedding）**：一个可学习的视觉嵌入表，将离散化的视觉 token 映射为连续嵌入向量，与文本 token 的嵌入维度对齐。
- **LLM（大语言模型）**：Qwen3-8B，负责融合视觉与文本信息并生成回答。

### 2.2 微调策略：部分模块 SFT

为了在有限算力下高效适配下游任务，**不训练全量参数**，仅训练以下三个模块：

| 训练模块 | 名称 | 作用 |
|:---|:---|:---|
| `visual_tokenizer.head` | 视觉投影头 | 负责将 ViT 特征映射到视觉词表空间，让模型更好地理解电力场景中的视觉模式 |
| `vte` | 视觉词嵌入表 | 将视觉 token 映射为 LLM 可理解的嵌入向量，适配电力领域的视觉语义 |
| `llm.model.layers.35` | LLM 最后一层 | 仅微调 Qwen3-8B 的最后一层 Transformer 层，以最小的参数代价注入电力领域知识 |

这种策略的优点：
- **参数效率高**：训练参数量远小于全量微调，降低显存和算力需求。
- **保留通用能力**：冻结大部分 LLM 参数，避免灾难性遗忘。
- **领域适配精准**：视觉侧模块（head + VTE）是跨模态对齐的关键，针对性训练即可取得良好效果。

### 2.3 训练超参数

| 参数 | 值 | 说明 |
|:---|:---|:---|
| `per_device_train_batch_size` | 1 | 单卡 batch size |
| `gradient_accumulation_steps` | 16 | 梯度累积步数 |
| 等效 batch size | 32 | 1 × 2 卡 × 16 |
| `num_train_epochs` | 8 | 训练轮数 |
| `learning_rate` | 2e-5 | 学习率 |
| `lr_scheduler_type` | cosine | 余弦退火 |
| `warmup_ratio` | 0.1 | 10% 步数预热 |
| `max_grad_norm` | 1.0 | 梯度裁剪 |
| `weight_decay` | 0.0 | 权重衰减 |
| `multimodal_max_length` | 6000 | 多模态样本最大 token 数 |
| `single_image_min_pixels` | 200704 | 图片最小像素数（≈448×448） |
| `single_image_max_pixels` | 3211264 | 图片最大像素数（≈1792×1792） |
| `bf16` | True | 混合精度训练 |
| `attn_implementation` | flash_attention_2 | 注意力加速 |
| `save_steps` | 0.4 | 每个 epoch 保存 2.5 个 checkpoint |
| `save_total_limit` | 2 | 仅保留最近 2 个 checkpoint |

### 2.4 训练启动命令

```bash
cd /data1/home/lwx/work/Code/Experiment/LingShu/Ovis
bash scripts/run_ovis2_5_sft.sh
```

训练脚本路径：`scripts/run_ovis2_5_sft.sh`

---

## 三、数据构造

### 3.1 数据集总览

本项目构建了覆盖电力输配电运维多个子领域的指令微调数据集，共 **6 个子数据集**，总计约 **9,894 条** 对话样本。

| 数据集名称 | 样本数 | 主题领域 | 数据文件 | 图片目录 |
|:---|:---|:---|:---|:---|
| `dxyw` | 806 | 导线异物（风筝、气球、鸟巢、塑料膜等） | `data_dxyw.jsonl` | `/data1/datasets/LingShu_DataSet/dataset/dxyw/image/` |
| `jyz` | 2,511 | 绝缘子与金具巡检（复合/玻璃/瓷绝缘子、线夹等） | `data_jyz.jsonl` | `/data1/datasets/LingShu_DataSet/dataset/jyz/image/` |
| `ybp` | 258 | 电表计量巡检（接线、铭牌、合规性） | `data_ybp.jsonl` | `/data1/datasets/LingShu_DataSet/dataset/ybp/image/` |
| `data_2_images` | 2,940 | 现场作业多图场景（2 图对比分析） | `data_2_images.jsonl` | `/data1/datasets/LingShu_DataSet/dataset/field_workers/2_images/image/` |
| `data_3_images` | 2,548 | 现场作业多图场景（3 图对比分析） | `data_3_images.jsonl` | `/data1/datasets/LingShu_DataSet/dataset/field_workers/3_images/image/` |
| `electric_power` | 831 | 电力通用场景 | `electric.jsonl` | 内置图片路径 |

### 3.2 数据格式

所有数据集统一采用 Ovis 的 **conversation** 格式（JSONL）：

```json
{
    "id": "aSCyQWzwXfHiBDjYxJ9U0kK72PZ6ldFIocEs4LVe",
    "img": "aSCyQWzwXfHiBDjYxJ9U0kK72PZ6ldFIocEs4LVe.jpg",
    "conversations": [
        {
            "from": "human",
            "value": "<image>\n图中所示为配电线路上悬挂的异物...请从电力运维角度分析..."
        },
        {
            "from": "gpt",
            "value": "图中显示一个红色灯笼状异物缠绕在多条架空导线上...\n\n从电力运维角度分析，其具体电气风险主要包括：\n1. **短路风险**...\n2. **电弧损伤**...\n3. **火灾风险**..."
        }
    ]
}
```

字段说明：
- `id`：样本唯一标识，与图片文件名一致（无扩展名部分）
- `img`：图片文件名（可支持多张图片，用列表表示）
- `conversations`：多轮对话列表
  - `from: "human"`：用户问题，`<image>` 占位符标记图片位置
  - `from: "gpt"`：标准回答，由电力领域专家编写

### 3.3 数据构造方法

#### 3.3.1 数据来源

- **真实巡检图片**：来源于电力公司无人机巡检、人工巡检拍摄的实际场景照片
- **领域专家标注**：每张图片的问题和回答均由电力运维专家撰写，保证内容的专业性和准确性

#### 3.3.2 问题设计原则

问题以**电力运维专家视角**设计，覆盖以下维度：

1. **缺陷识别**：识别图片中的异常（异物、鸟巢、破损等）
2. **风险分析**：分析该缺陷可能引发的电气故障（短路、闪络、污闪、火灾等）
3. **处置方案**：阐述标准作业流程（SOP）、安全规程、技术手段
4. **预防措施**：长效防治方案（驱鸟装置、线路设计优化等）

#### 3.3.3 回答质量标准

回答具有以下特征：
- **结构化**：分点论述，条理清晰
- **专业性**：使用标准电力术语（如相间短路、单相接地、绝缘闪络、爬电距离等）
- **全面性**：涵盖电气风险、机械风险、安全风险、处置流程、预防措施
- **场景化**：紧密结合图片中的具体设备和场景

#### 3.3.4 典型场景举例

| 场景类型 | 典型案例 | 核心分析点 |
|:---|:---|:---|
| 风筝挂线 | 风筝缠绕 10kV/110kV 线路 | 相间短路风险、停电处置 SOP、无人机清除技术 |
| 鸟巢隐患 | 铁塔横担大型鸟巢 | 绝缘距离降低、污闪、驱鸟装置选型 |
| 气球/薄膜缠绕 | 铝箔气球、塑料大棚膜 | 导电性分析、激光清障 vs 喷火无人机对比 |
| 绝缘子缺陷 | 复合绝缘子老化、玻璃绝缘子自爆 | 憎水性下降、芯棒脆断、零值检测 |
| 电表巡检 | 老旧电表、接线不规范 | 计量合规性、铭牌参数解读、安全风险 |

### 3.4 数据集注册

数据集通过 `ovis/train/dataset/ovis2_5_sft_datainfo.json` 统一注册管理：

```json
{
    "dxyw": {
        "meta_file": "data_dxyw.jsonl",
        "storage_type": "hybrid",
        "data_format": "conversation",
        "image_dir": "/data1/datasets/LingShu_DataSet/dataset/dxyw/image/"
    },
    ...
}
```

训练脚本通过 `--data_name "dxyw|jyz|ybp|data_2_images|data_3_images|electric_power"` 指定加载哪些数据集。

---

## 四、项目文件结构

```
Ovis/
├── ovis/
│   ├── model/
│   │   ├── modeling_ovis.py          # Ovis 模型主体定义
│   │   ├── configuration_ovis.py     # 模型配置
│   │   ├── conversation_formatter.py # 对话模板格式化
│   │   └── vit/                      # SigLIP2 ViT 实现
│   ├── train/
│   │   ├── train.py                  # 训练主入口
│   │   ├── arguments.py              # 训练参数定义
│   │   ├── callback.py               # 训练回调
│   │   └── dataset/
│   │       ├── conversation_dataset.py   # 对话数据加载
│   │       ├── multimodal_dataset.py     # 多模态数据基类
│   │       └── ovis2_5_sft_datainfo.json # 数据集注册
│   ├── serve/                        # 推理 demo
│   └── util/                         # 工具函数
├── scripts/
│   └── run_ovis2_5_sft.sh            # 训练启动脚本
├── data_dxyw.jsonl                   # 导线异物数据集
├── data_jyz.jsonl                    # 绝缘子金具数据集
├── data_ybp.jsonl                    # 电表数据集
├── data_2_images.jsonl               # 双图场景数据集
├── data_3_images.jsonl               # 三图场景数据集
├── electric.jsonl                    # 电力通用数据集
├── output/checkpoints/               # 训练 checkpoint 输出
├── README.md                         # Ovis 官方文档
└── requirements.txt                  # 依赖
```

---

## 五、Checkpoint 与推理

### 5.1 Checkpoint 路径

训练输出目录：`output/checkpoints/sft_v2/`（由脚本中 `EXPNAME="sft_v2"` 定义）

### 5.2 推理方式

可使用 Ovis 官方推理脚本加载微调后的 checkpoint：

```bash
# Web UI 推理
python ovis/serve/web_ui.py --model-path output/checkpoints/sft_v2/checkpoint-XXXX --port 8001

# 或使用 vLLM 部署
vllm serve output/checkpoints/sft_v2/checkpoint-XXXX --trust-remote-code --port 8000
```

微调后的模型支持 thinking mode（`enable_thinking=True`）以启用反思推理。

---

## 六、后续工作建议

1. **数据扩展**：可继续增加更多电力场景数据（如变电站设备、电缆接头、杆塔基础等），扩大领域覆盖范围。
2. **评估体系**：建议建立电力领域专项评测集，评估模型在不同子任务（缺陷分类、风险定级、方案生成）上的表现。
3. **模型部署**：微调后的 checkpoint 可转换为 vLLM 格式或 GPTQ 量化版本，以便在生产环境中高效推理。
4. **持续迭代**：可尝试训练更多 LLM 层（如 layers.34~35）、调整学习率、引入 DPO 对齐训练以进一步提升效果。

---

## 七、关键联系人与资源

- **Ovis 模型官网**：https://github.com/AIDC-AI/Ovis
- **Ovis2.5 技术报告**：https://arxiv.org/abs/2508.11737
- **预训练权重**：`/data1/home/lwx/work/Download/modal_models/ovis`
- **训练数据图片根目录**：`/data1/datasets/LingShu_DataSet/dataset/`
